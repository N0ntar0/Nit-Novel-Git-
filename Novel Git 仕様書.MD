

# Novel-Git (仮)

執筆者の「迷い」を資産に変える、バージョン管理機能付き小説エディタ

## 1. プロダクトコンセプト

### 1.1 背景と目的

文芸学部での創作活動と、システム部門での技術経験を融合させ、従来のテキストエディタにはない「執筆プロセスの可視化」を実現する。プログラミングにおけるGitの利便性を、小説執筆という領域に合わせて再定義する。

### 1.2 ターゲットユーザー

* 推敲を重ねる小説家・シナリオライター
* 過去の没稿や別ルートのアイデアを捨てられず、管理に困っている執筆者
* 縦書きでの執筆・推敲を重視するユーザー

---

## 2. 技術スタック

* **Frontend:** React (Vite), TypeScript
* **Styling:** Tailwind CSS (縦書き/横書きの動的切り替え)
* **State Management:** Zustand (軽量かつ高速な状態管理)
* **Database:** IndexedDB (Dexie.js) ※完全ローカル動作によるプライバシー確保
* **Deployment:** GitHub Pages

---

## 3. 機能要件

### 3.1 エディタ機能

* **ハイブリッドレイアウト:** CSS `writing-mode` を用いた縦書き・横書きのワンタッチ切り替え。
* **オートセーブ:** 執筆中の内容をブラウザに自動保存。
* **文字数・段落数カウント:** 執筆状況のリアルタイム表示。

### 3.2 バージョン管理（スナップショット）

* **チェックポイント保存:** ユーザーが任意のタイミングで「保存ポイント」を作成。
* **コミットメッセージ:** 保存時に変更意図（例：「主人公の葛藤を強調」）を記録。
* **履歴タイムライン:** 左サイドバーにて過去のバージョンを一覧表示。

### 3.3 比較検討（Diff）機能

* **階層的ディファレンシング:**
* **第1階層（段落単位）:** 改行コードによる分割比較。大きな構成の変化を特定。
* **第2階層（句読点単位）:** 変更があった段落内をさらに句読点で分割比較。一文ごとの細かな修正を特定。


* **サイドバイサイド・ビュー:** 過去稿（右）と現行稿（左）を並列表示。縦書き時は日本語の視線誘導に基づき右から左へ配置。
* **同期スクロール:** 左右の画面で同じ段落位置を保持。

---

## 4. UI/UX 仕様

### 4.1 比較画面のインターフェース

* **アノテーション・ガーター:** エディタ横の領域に変更アイコンを表示。
* **詳細ポップアップ:** アイコンクリック時、句読点レベルの差分を吹き出しで表示し、執筆のノイズを最小化する。

### 4.2 縦書き対応

* **スクロール方向変換:** 縦書きモード時、マウスホイールの上下操作を横スクロールに自動変換するUXを実装。

---

## 5. データ構造設計

### 5.1 Snapshot オブジェクト

```typescript
interface Snapshot {
  id: string;
  timestamp: number;
  message: string;        // 保存時のメモ
  content: string;        // 本文全文
  layoutMode: 'vertical' | 'horizontal';
}

```

### 5.2 Diff データ構造

```typescript
interface DiffResult {
  paragraphs: {
    index: number;
    status: 'unchanged' | 'modified' | 'added' | 'deleted';
    text: string;
    details?: SentenceDiff[]; // 句読点単位の詳細
  }[];
}

```

---

## 6. 開発ロードマップ

1. **Phase 1: 最小機能の実装 (MVP)**
* React + IndexedDB による基本エディタの構築
* チェックポイント保存と履歴一覧の表示


2. **Phase 2: 比較エンジンの実装**
* 段落単位の比較ロジック構築
* 縦書き対応サイドバイサイド・ビューの実装


3. **Phase 3: 洗練とUX向上**
* 句読点単位の詳細比較機能
* アノテーションUIの実装
* GitHub Pagesへのデプロイ